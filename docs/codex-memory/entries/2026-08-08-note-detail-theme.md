# 笔记详情页主题背景修复

## 问题

Skyline 下笔记详情页出现深色标题和操作栏叠在浅色背景上，标题几乎不可读。

## 原因与处理

- 详情页及分类管理页使用了 `var(--page-bg)`，但全局没有定义该变量，导致背景声明失效。
- 为根页面和日间/夜间主题补齐 `--page-bg` 渐变变量，确保主题文字、控件和页面背景同步。
- 增加笔记详情主题背景回归测试，避免再次出现半暗半亮状态。

## 相关文件

- `app.wxss`
- `pages/note/edit/index.wxss`
- `tests/note-page.test.js`
