# 小程序笔记会话解绑失效（已被统一 token 方案替代）

历史方案曾让笔记请求带 `x-note-session: 1`，并用 `ltw_note_token` 绑定微信 openid。该方案已撤销：现在所有入口共用 `ltw_token` 普通用户 JWT，笔记权限只按用户 token 判断，微信解绑不会删除账号或阻止无微信账号使用笔记。页面仍会在普通 token 失效时清空笔记状态并引导登录。
