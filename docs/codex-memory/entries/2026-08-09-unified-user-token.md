# 笔记与个人中心统一用户认证

小程序只保留 `ltw_token` 普通用户 token；请求层统一通过 `web_token` 发送，不再发送 `x-note-session`，笔记页没有 token 时直接提示去“我”页登录。个人中心有 token 时调用 `/user/info`，没有 token 时才调用微信登录/微信用户信息接口；微信绑定只是资料来源之一，不是笔记权限条件。个人中心提供账号密码登录、已绑定微信登录和退出登录，退出会清理小程序 token，并触发笔记状态刷新；注册成功也保存普通 token。
